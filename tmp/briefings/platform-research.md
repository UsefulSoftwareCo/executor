```json
{
  "pins": [
    "node_modules/eve/package.json",
    "node_modules/eve/docs/concepts/sessions-runs-and-streaming.md",
    "node_modules/eve/docs/concepts/execution-model-and-durability.md",
    "node_modules/eve/docs/guides/hooks.md",
    "node_modules/eve/docs/skills.mdx",
    "node_modules/eve/docs/guides/dynamic-capabilities.md",
    "node_modules/eve/docs/channels/slack.mdx",
    "node_modules/eve/docs/subagents.mdx",
    "node_modules/eve/dist/src/client/index.d.ts",
    "node_modules/eve/dist/src/public/channels/slack/index.d.ts",
    "node_modules/eve/dist/src/public/hooks/index.d.ts",
    "node_modules/eve/dist/src/public/skills/index.d.ts"
  ],
  "briefing": "Research-only worker context. Repo root is /home/dana/projects/augment/agents/executor; do not map app code. Re-read the pinned Eve files verbatim because installed eve is 0.18.2 and vendors its own docs/types.\n\nEve invariants: Eve sessions expose two distinct handles: continuationToken resumes/sends follow-ups and is owned by the channel; sessionId/run stream handles inspect/attach to event streams and are runtime-owned. A session has one active continuation token at a time; stale tokens are rejected. Stream API is NDJSON at /eve/v1/session/:sessionId/stream and replayable via startIndex. Relevant events include session.started, turn.started/completed/failed, message.* streaming/completed, actions.requested/action.result, input.requested, authorization.*, session.waiting/completed/failed, subagent.called/completed. Source: https://eve.dev/docs/concepts/sessions-runs-and-streaming. Multiplayer/read-only attach: docs explicitly allow clients to attach/reconnect to a session stream by sessionId and childSessionId for subagents, but sending follow-ups should be serialized; for deterministic ordering send one follow-up and wait for session.waiting. Treat multi-client watching as supported by durable replayable stream, but multi-writer chat as constrained by single active continuation token. Source: https://eve.dev/docs/concepts/sessions-runs-and-streaming.\n\nEve hooks/capabilities: hooks live under agent/hooks and subscribe to runtime stream events after the event is durably recorded; handler failures can surface as turn.failed/session.failed, so hooks are real runtime behavior. Hooks observe side effects and cannot inject model context; use tools/context/dynamic instructions for that. Source: https://eve.dev/docs/guides/hooks. Dynamic capabilities are defineDynamic resolvers for tools, skills, and instructions keyed on session.started/turn.started/step.started (step only for tools). Dynamic tools/skills can override authored ones; dynamic-vs-dynamic name collisions throw. Inline execute functions are required for durable replay. Source: https://eve.dev/docs/guides/dynamic-capabilities. Skills are load-on-demand SKILL.md procedures exposed through load_skill; they add instructions, not new execution surfaces; subagent skills are scoped and not shared. Source: https://eve.dev/docs/skills.\n\nEve Slack: Slack channel supports app mentions and DMs, thread anchoring, typing/progress indicators, HITL buttons/selects, authorization challenge private delivery, and proactive sessions via receive(slack, {message,target,auth}). Credentials are via Vercel Connect, not raw SLACK_BOT_TOKEN/SIGNING_SECRET in app code. onAppMention/onDirectMessage decide dispatch and auth; default auth is workspace-scoped. Eve attaches triggering Slack user id to the model message; optional threadContext fetches previous thread replies via conversations.replies and attributes by stable Slack user id. Source: https://eve.dev/docs/channels/slack. Local types to inspect: node_modules/eve/dist/src/public/channels/slack/index.d.ts.\n\nEve multi-agent/handoff: built-in agent tool delegates to a copy of the current agent with shared sandbox/tools and fresh history/state; multiple agent tool calls in one response fan out concurrently. Declared subagents live under agent/subagents/<id>, have their own instructions/tools/connections/skills/sandbox/hooks, inherit no root authored slots, and are exposed as tools with {message, outputSchema?}. Parent stream only emits subagent.called/subagent.completed; attach to childSessionId stream for progress. Default max subagent depth is 3. Source: https://eve.dev/docs/subagents. Remote agents are referenced from that page but were not fetched; fetch https://eve.dev/docs/guides/remote-agents if remote handoff matters.\n\nTipTap collaboration: Current docs position Collaboration as real-time editing with presence/awareness, version history, comments, every change stored as a Yjs update, REST APIs/webhooks for document management, and migration between Hocuspocus/self-hosted and Tiptap Collaboration Cloud/on-prem via provider switch. Source: https://tiptap.dev/docs/collaboration/getting-started/overview. Hocuspocus is the open-source Yjs WebSocket backend, integrates with existing infra, has webhooks, Redis scaling, and runs on Node.js, Bun, Deno, and Cloudflare Workers since v4. Source: https://tiptap.dev/docs/hocuspocus/introduction. For a self-hosted Notion-like editor, plan for: Tiptap editor + Collaboration extension using Yjs, a WebSocket collaboration server (Hocuspocus or y-websocket), persistence of Yjs updates/snapshots, awareness/presence, auth/room authorization, schema/version migration handling, comments/thread storage or licensed Tiptap Comments, and backups/export via document APIs if using Tiptap cloud/on-prem.\n\nTipTap comments licensing: Comments extension is @tiptap-pro/extension-comments from Tiptap private npm registry; docs say activate trial or subscribe and use CommentsKit with TiptapCollabProvider; threads are a cloud or on-premises feature. Source: https://tiptap.dev/docs/comments/getting-started/install. Search result also indicated Comments overview requires Start plan, while Comments REST API/webhooks require Team plan; verify exact plan matrix on current pricing/dashboard before committing.\n\nYjs/Postgres persistence: Yjs update model supports full-state updates via Y.encodeStateAsUpdate(doc) and incremental update persistence via doc.on('update', update => persist update). Community guidance says minimum SQL shape can be docName + blob update rows; updates can be merged after a threshold to save space; a Postgres provider can be modeled after y-leveldb. Sources: https://discuss.yjs.dev/t/sql-graphql-backend-where-do-i-start/2194 and https://discuss.yjs.dev/t/syncing-document-to-postgres-database/1793. y-postgresql exists as a Postgres adapter for y-websocket; README pattern uses bindState to load persisted Y.Doc, store current/new updates, apply persisted state, listen to ydoc update events, and writeState on doc teardown. Source: https://github.com/MaxNoetzold/y-postgresql. Implementation pattern to brief: append raw binary updates in Postgres for durability, periodically compact/merge updates into a snapshot/full Yjs state to cap load time, keep state vectors or clocks for efficient diffing, and make snapshot/update writes transactionally ordered per doc.\n\nSlack membership/user mapping: conversations.members returns paginated Slack user IDs for a channel/conversation; use cursor pagination and recommended limit <= 200 even though max is under 1000. It requires a token with required scopes and can return missing_scope/no_permission; the app must be a member for some conversations. Source: https://api.slack.com/methods/conversations.members. Required scopes depend on conversation type; verify on the app manifest, but expect channels:read for public channels, groups:read for private channels, im:read/mpim:read for IM/MPIM visibility, plus users:read/users:read.email when resolving user profiles/emails. users.lookupByEmail requires users:read.email and custom bot users cannot use it; deactivated users return users_not_found and require users.list filtering. Source: https://api.slack.com/methods/users.lookupByEmail. At scale, map by (enterprise_id/team_id, slack_user_id) as the stable primary external identity; use email only as bootstrap/claim signal because emails may be hidden, changed, reused, or unavailable. Cache profiles and membership with cursor sync plus Events API deltas where available; handle Grid/workspace boundaries explicitly.\n\nVercel/WebSockets 2026: Vercel Functions can serve WebSocket connections for realtime/chat/collaborative apps; WebSockets require Fluid compute, default for new projects created on/after Apr 23 2025. One WebSocket is pinned to one Function instance for its lifetime; Fluid compute lets one instance handle multiple connections. Reconnects are not guaranteed to hit the same instance, deployments can split old/new connections, and durable state/presence/rooms/pubsub coordination must be stored externally (e.g. Redis). Source: https://vercel.com/docs/functions/websockets. Next.js does not expose native WebSocket upgrade handling; workaround is @vercel/functions experimental_upgradeWebSocket. Source: https://vercel.com/docs/functions/websockets. WebSockets close when the Function reaches max duration; function limits show 300s default, 800s max Pro/Ent, 1800s extended beta for eligible Node/Python. Source: https://vercel.com/docs/functions/limitations. Conclusion: in 2026 Vercel can host WebSocket endpoints, but a production Yjs/Hocuspocus server needs external durable pub/sub/persistence and reconnect handling; for long-lived collaboration rooms, a separate always-on/realtime service, Hocuspocus Cloud/on-prem, Partykit-style host, or Vercel Services + Redis/Postgres architecture may be operationally cleaner than a plain Next.js route.\n\nGAPS: Need a fresh fetch of https://eve.dev/docs/guides/remote-agents if remote multi-agent handoff is in scope. Need exact Slack scope table from current Slack OAuth method docs/app manifest because scope pages fetched poorly; method pages only prove missing_scope behavior and users.lookupByEmail users:read.email. Need exact Tiptap plan/licensing matrix for Comments/REST/webhooks from pricing or dashboard; docs prove private pro package and trial/subscription requirement. Need current package types if workers actually install @tiptap-pro/extension-comments, @hocuspocus/server/provider, y-postgresql, yjs, y-websocket; this repo search showed Tiptap packages and Eve but not necessarily collaboration server packages."
}
```
