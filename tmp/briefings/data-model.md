```json
{
  "pins": [
    "apps/open-agents/lib/db/schema.ts",
    "apps/open-agents/lib/db/auth-schema.ts",
    "apps/open-agents/lib/auth/config.ts",
    "agent/lib/open-agents-profile.ts"
  ],
  "briefing": "Repo root: /home/dana/projects/augment/agents/executor. Start from docs/open-agents-operations-handoff.md; it identifies Open Agents as apps/open-agents plus Eve agent code and names the persistence-critical tables.\n\nLoad-bearing files: apps/open-agents/lib/db/schema.ts is the canonical Drizzle app schema for Open Agents tables and types; apps/open-agents/lib/db/auth-schema.ts is the Better Auth schema; apps/open-agents/lib/auth/config.ts wires Better Auth providers and model mappings; agent/lib/open-agents-profile.ts is the Eve dynamic profile resolver that reads sessions + agent_library_items via raw postgres.\n\nDB clients/helpers: apps/open-agents/lib/db/sql.ts creates the singleton postgres client from POSTGRES_URL with max=1, idle_timeout=10, application_name open-agents:${NODE_ENV}:pid...:pool...; apps/open-agents/lib/db/client.ts exports Drizzle db over schema.ts; apps/open-agents/lib/auth/db.ts exports Drizzle authDb over auth-schema.ts. Table-specific helpers live in apps/open-agents/lib/db/: sessions.ts (sessions/chats/shares/chat_reads), eve-chat-sessions.ts (eve_chat_session_states/eve_chat_events), slack-user-links.ts, installations.ts, users.ts, user-preferences.ts, usage.ts, vercel-project-links.ts, last-repo.ts, public-usage-profile.ts, usage-insights.ts, usage-domain-leaderboard.ts. Important exception: agent/lib/open-agents-slack-session.ts and agent/lib/open-agents-profile.ts bypass Drizzle and use raw postgres(process.env.POSTGRES_URL!, max=1, idle_timeout=10).\n\nMigrations: Open Agents migrations are under apps/open-agents/lib/db/migrations plus meta snapshots. Latest snapshot verified: apps/open-agents/lib/db/migrations/meta/0051_snapshot.json. Journal has entries 0000_wakeful_wolfpack through 0051_quick_professor_monster in apps/open-agents/lib/db/migrations/meta/_journal.json. Notable migrations: 0000 created legacy users/accounts; 0029_amazing_galactus renamed legacy accounts to github_accounts and created Better Auth accounts/auth_sessions/verification; 0030_better_auth_backfill backfilled Vercel Better Auth accounts; 0032_ambitious_silverclaw dropped legacy user auth columns; 0032_funny_tag added users.email_verified; 0033 dropped github_accounts; 0034 added users.is_admin; 0039 added automation tables; 0041_sudden_puff_adder added agent_library_items; 0041_salty_stryfe added scope_kind/scope_id to agent_library_items; 0042 added slack_user_links; 0043 added Eve chat state/events; 0049 created open_agents_executor_* runtime tables and dropped un-namespaced legacy executor tables; 0051 added slack_thread_sessions.\n\nCurrent tables in latest snapshot: accounts, agent_library_items, auth_sessions, automation_approvals, automation_artifacts, automation_correlations, automation_definitions, automation_events, automation_invocations, automation_message_queue, automation_outbox, automation_run_attempts, automation_runs, automation_state, automation_timeline_events, automation_versions, chat_reads, chats, eve_chat_events, eve_chat_session_states, github_installations, open_agents_executor_blob, open_agents_executor_connection, open_agents_executor_definition, open_agents_executor_integration, open_agents_executor_oauth_client, open_agents_executor_oauth_session, open_agents_executor_plugin_storage, open_agents_executor_tool, open_agents_executor_tool_policy, private_open_agents_executor_settings, sessions, shares, slack_thread_sessions, slack_user_links, usage_events, user_preferences, users, vercel_project_links, verification.\n\nUsers/auth model: apps/open-agents/lib/db/auth-schema.ts defines users(id text PK, username text not null, email text, email_verified boolean default false, name text, avatar_url text, is_admin boolean default false, created_at, updated_at, last_login_at), accounts(id, account_id, provider_id, user_id FK users.id cascade, access/refresh/id tokens, token expiry timestamps, scope, password, created/updated), auth_sessions(id, expires_at, token unique, ip_address, user_agent, user_id FK), verification(id, identifier, value, expires_at, created/updated). apps/open-agents/lib/auth/config.ts uses betterAuth with drizzleAdapter(authDb, provider pg) and maps schema users/auth_sessions/account/verification. Better Auth user modelName is users; image field maps to avatarUrl; additional fields are username required string and lastLoginAt date. Session modelName is auth_sessions. Account config encrypts OAuth tokens and enables accountLinking for trustedProviders [vercel, github] with allowDifferentEmails=true. Providers: vercel uses NEXT_PUBLIC_VERCEL_APP_CLIENT_ID, VERCEL_APP_CLIENT_SECRET, scopes openid/email/profile/offline_access, overrideUserInfoOnSignIn=true, mapProfileToUser derives username from sub/preferred_username/email/name. github uses NEXT_PUBLIC_GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, mapProfileToUser derives username from id/login/email/name. IDs generated by nanoid.\n\nLocal/auth request shape: apps/open-agents/lib/session/local-development-session.ts enables local auth when NODE_ENV !== production and OPEN_AGENTS_AUTH_MODE=local, upserts users row with id OPEN_AGENTS_LOCAL_USER_ID or local-user, emailVerified true, isAdmin true. apps/open-agents/lib/session/get-server-session.ts and server.ts return local session in that mode, else auth.api.getSession(headers). agent/channels/eve.ts accepts four auth strategies: openAgentsLocalSession, openAgentsWebSession via auth.api.getSession, vercelOidc with trusted x-open-agents-user-id, and localDev with trusted x-open-agents-user-id. It copies x-open-agents-session-id/chat-id/tool-profile into Eve auth attributes.\n\nOrg/team/group/membership concepts: no first-class org/team/group/membership tables were found in the Open Agents DB snapshot. Existing scoping concepts are string columns only: agent_library_items.scope_kind enum user|org plus scope_id; automation_definitions.scope_kind enum system|user|thread|session|repo|automation|external-thread plus scope_id and owner_kind/owner_id; automation_events.scope_kind enum system|user|thread|session|repo|automation plus scope_id; sessions/vercel_project_links store Vercel team metadata as vercel_team_id/vercel_team_slug or team_id/team_slug; github_installations.account_type enum User|Organization. agent/lib/open-agents-profile.ts hardcodes DEFAULT_ORG_SCOPE_ID='default' unless OPEN_AGENTS_ORG_SCOPE_ID is set; no org membership lookup exists there.\n\nOwnership/scoping columns by major entity: sessions has user_id FK users.id not null plus repo metadata and vercel_team_id/vercel_team_slug; indexed sessions_user_id_idx. chats has session_id FK sessions.id cascade, no direct user_id; scope is inherited through sessions. eve_chat_session_states has chat_id PK/FK chats.id cascade only; scope inherited via chat->session. eve_chat_events has chat_id FK + stream_index PK, event_type text, event jsonb; scope inherited via chat->session. shares has chat_id unique FK only; no direct user_id. chat_reads has user_id + chat_id composite PK. github_installations has user_id FK and installation/account fields. slack_user_links has user_id FK plus slack_team_id/slack_user_id unique identity. slack_thread_sessions has slack_team_id/slack_channel_id/slack_thread_ts composite PK plus user_id/session_id/chat_id FKs and unique chat_id. vercel_project_links has composite PK user_id/repo_owner/repo_name plus team_id/team_slug. user_preferences has unique user_id FK. usage_events has user_id FK. agent_library_items has user_id FK plus scope_kind/scope_id/kind/item_id; user_id remains required even for org-scoped rows, while profile reads use scope_kind/scope_id. automation_definitions has scope_kind/scope_id and owner_kind/owner_id; automation_versions has automation_id FK and created_by text; automation_events has scope_kind/scope_id, subject_kind/subject_id, actor_json, connector/installation ids; automation_invocations/runs/attempts/state/correlations/artifacts/approvals/timeline/outbox scope through automation_id/run_id/event_id, with runs/correlations optionally linking session_id/chat_id. automation_message_queue has run_id optional plus session_id/chat_id/user_id FKs. Executor runtime tables: open_agents_executor_connection/definition/oauth_client/oauth_session/plugin_storage/tool/tool_policy all have tenant, owner, subject columns and unique indexes including those; open_agents_executor_integration has tenant only; open_agents_executor_blob has namespace/key/id/row_id and no tenant/owner/subject; private_open_agents_executor_settings has id/version only.\n\nTables with no direct ownership/scoping columns today: auth_sessions/verification/accounts are auth-owned only via user_id where present; verification has no user_id. chats, eve_chat_session_states, eve_chat_events, shares inherit through chat/session and have no user_id. automation_invocations, automation_run_attempts, automation_state, automation_artifacts, automation_approvals, automation_timeline_events, automation_outbox inherit through automation/run and have no user_id/owner columns. open_agents_executor_blob and private_open_agents_executor_settings have no tenant/owner/subject. No docs table exists in the latest Open Agents snapshot; any docs persistence is not represented as a dedicated apps/open-agents DB table.\n\nSlack mapping: apps/open-agents/lib/db/slack-user-links.ts normalizes slackTeamId to trimmed value or 'default', slackUserId to uppercase, and upserts by unique user_id with unique slack_team_id/slack_user_id. apps/open-agents/app/api/settings/slack-link/route.ts exposes GET/PUT/DELETE for the current app user using getSessionFromReq; PUT rejects duplicate Slack identity by catching slack_user_links_identity_idx. agent/lib/open-agents-slack-session.ts maps inbound Slack user to app user by exact slack_user_links(slack_team_id, slack_user_id), falling back to slack_team_id='default' only when the incoming team is not default. If no link exists, getOrCreateOpenAgentsSlackSession returns null. New Slack threads insert sessions(user_id, title, status='running', repo metadata or workspace_repos, sandbox_state {type:'vercel'}, lifecycle_state='provisioning', lifecycle_version=0), chats(title='Slack', model_id='anthropic/claude-sonnet-4.6'), and slack_thread_sessions(team/channel/thread,user_id,session_id,chat_id) in one transaction. Existing Slack threads are looked up by team/channel/thread and point back to existing session/chat. markOpenAgentsSlackSessionLinkPosted sets link_posted_at.\n\nEve chat persistence shape: apps/open-agents/lib/db/schema.ts defines eve_chat_session_states(chat_id PK/FK chats.id cascade, state jsonb typed SessionState, created_at, updated_at) and eve_chat_events(chat_id FK, stream_index integer, event_type text, event jsonb typed HandleMessageStreamEvent, created_at, primary key chat_id+stream_index). apps/open-agents/lib/db/eve-chat-sessions.ts creates initial state {streamIndex:0}; snapshot returns {session, events, isStreaming}; streaming is latest event exists and !isCurrentTurnBoundaryEvent(event). buildEveChatEventRows uses firstStreamIndex+offset and event.type. persistEveChatSessionPatch inserts events on conflict do nothing, updates chats.updated_at and last_assistant_message_at when any event.type==='message.completed', and upserts session state. Slack has parallel raw-SQL versions in agent/lib/open-agents-slack-session.ts that persist cursor immediately after Client.session(...).send() and then persist each stream event as it arrives.\n\nDynamic agent/skill profile: agent_library_items stores kind agent|skill, item_id, item_json, user_id, scope_kind user|org, scope_id. agent/lib/open-agents-profile.ts reads session by id from sessions selecting user_id, agent_name, workspace_repos and rejects it if Eve auth userId is present and differs. It resolves agent by searching scopes in order user:userId then org:OPEN_AGENTS_ORG_SCOPE_ID/default. Skill listing reverses scopes so user skills override org skills in a Map. Agent item_json shape expected by resolver: {slug,name,description,tools,skills,systemPrompt}. Skill item_json shape: {id,name,description,body}. Supported dynamic tools are todo, read_file, write_file, grep, glob, bash, web_fetch, load_skill. Header x-open-agents-tool-profile can override tools.\n\nGotchas for refactors: Drizzle schema.ts does not include open_agents_executor_* tables even though the latest migration snapshot does; those are created by SQL migration 0049 and likely owned by embedded Executor runtime. agent_library_items has both user_id and scope_kind/scope_id, so org-scoped rows still need a user_id FK. chats/Eve/shares are easy to leak if code does not join through sessions.user_id. Slack-created sessions rely on slack_user_links existing; Slack user identity normalization uppercases user IDs and team fallback to default is asymmetric. Better Auth users are no longer provider-scoped in users; provider identity lives in accounts.account_id/provider_id. Local dev auth mutates users and creates an admin local-user row.\n\nGAPS: I verified source/schema/migration files and helper locations, but did not query a live database for runtime-only drift beyond migration snapshot 0051. I did not fully inspect every API route that calls the helpers; ownership enforcement at route level needs a separate pass. I found no dedicated docs table in Open Agents migrations/snapshot; if docs are stored outside Postgres or inside Executor plugin_storage/item_json, that requires product-specific tracing. The exact package code that creates/uses open_agents_executor_* tables was not fully mapped; only the Open Agents migration shape was verified."
}
```
