```json
{
  "pins": [
    "docs/open-agents-operations-handoff.md",
    "agent/agent.ts",
    "agent/channels/eve.ts",
    "agent/channels/slack.ts",
    "agent/lib/open-agents-profile.ts",
    "agent/lib/open-agents-slack-session.ts",
    "agent/tools/open_agents_profile.ts",
    "agent/instructions/open_agents_profile.ts",
    "apps/open-agents/lib/db/schema.ts",
    "node_modules/eve/README.md",
    "node_modules/eve/docs/subagents.mdx",
    "node_modules/eve/docs/guides/remote-agents.md",
    "node_modules/eve/dist/src/public/definitions/agent.d.ts",
    "node_modules/eve/dist/src/public/definitions/remote-agent.d.ts",
    "node_modules/eve/dist/src/public/definitions/tool.d.ts",
    "node_modules/eve/dist/src/shared/dynamic-tool-definition.d.ts",
    "node_modules/eve/dist/src/client/client.d.ts",
    "node_modules/eve/dist/src/client/session.d.ts",
    "node_modules/eve/dist/src/runtime/actions/types.d.ts",
    "node_modules/eve/dist/src/channel/types.d.ts"
  ],
  "briefing": "Map of Eve/Open Agents runtime under /home/dana/projects/augment/agents/executor.\n\nEve root config: agent/agent.ts default-exports defineAgent({ model: \"anthropic/claude-sonnet-4.6\", modelOptions.providerOptions.anthropic: { effort: \"medium\", thinking: { type: \"adaptive\" } }, modelContextWindowTokens: 200_000, compaction.thresholdPercent: 0.75, build.externalDependencies: [\"postgres\"] }). The installed Eve agent-config docs say defineAgent identity is filesystem/package-derived, not a name field; compaction threshold controls when older turns summarize; build.externalDependencies keeps runtime deps external. Type signature: node_modules/eve/dist/src/public/definitions/agent.d.ts exports AgentDefinition = PublicAgentDefinition and defineAgent<TAgent extends AgentDefinition>(definition: ExactDefinition<TAgent, AgentDefinition>): TAgent.\n\nChannels: agent/channels/eve.ts wraps eveChannel({ auth: [...] }). Auth chain is, in order: openAgentsLocalSession(), openAgentsWebSession(), vercelOidc() with trustOpenAgentsUserHeader, and localDev() with trustOpenAgentsUserHeader. withOpenAgentsRequestAttributes copies x-open-agents-session-id -> auth.attributes.openAgentsSessionId, x-open-agents-chat-id -> openAgentsChatId, x-open-agents-tool-profile -> openAgentsToolProfile; when trustOpenAgentsUserHeader is true it also sets subject from x-open-agents-user-id. openAgentsLocalSession only works outside production and when OPEN_AGENTS_AUTH_MODE === \"local\"; openAgentsWebSession uses apps/open-agents/lib/auth/config auth.api.getSession({ headers }).\n\nSlack channel flow: agent/channels/slack.ts default-exports slackChannel({ onAppMention, onDirectMessage, threadContext: { since: \"last-agent-reply\" } }). handleSlackMessage drops messages without author, prompts when no text/attachments, starts typing, then calls getOrCreateOpenAgentsSlackSession. If no linked Open Agents user, it replies with Slack team/user IDs. For a new session it posts the session URL, marks link posted, initializes sandbox, posts \"Workspace sandbox is ready.\", then calls runOpenAgentsSlackTurn. Busy turns reply \"I am still working...\"; otherwise it posts the final assistant message or fallback.\n\nSlack DB/session implementation: agent/lib/open-agents-slack-session.ts uses postgres(process.env.POSTGRES_URL!, { max: 1, idle_timeout: 10 }) cached on globalThis. User link lookup reads slack_user_links by slack_team_id + normalized uppercase slack_user_id, with fallback to slack_team_id='default'. Existing thread lookup reads slack_thread_sessions by composite key slack_team_id/slack_channel_id/slack_thread_ts. New Slack sessions insert into sessions, chats, and slack_thread_sessions in a transaction. New sessions use nanoid ids, title from cleaned Slack text, status='running', sandbox_state { type: \"vercel\" }, lifecycle_state='provisioning', lifecycle_version=0, chats.title='Slack', chats.model_id='anthropic/claude-sonnet-4.6'. Repo targeting parses github.com/owner/repo or `repo: owner/repo`; otherwise default workspace repos come from apps/open-agents/lib/workspace-repos. Sandbox init calls @open-agents/sandbox connectSandbox with persistent Vercel sandbox name session_<sessionId>, default ports [3000,5173,4321,8000], vcpus=4, OPEN_AGENTS_GITHUB_TOKEN, then updates sessions.sandbox_state, lifecycle_state='active', lifecycle_error=null, hibernate_after, sandbox_expires_at, lifecycle_version+1.\n\nSlack Eve turn: runOpenAgentsSlackTurn reads latest eve_chat_events row for chat_id and rejects if latest event is not isCurrentTurnBoundaryEvent. It reads eve_chat_session_states.state or uses { streamIndex: 0 }, creates new eve/client Client({ host: public base URL, preserveCompletedSessions: true, redirect: \"manual\", auth: Vercel OIDC only on Vercel }), then client.session(initialState).send({ message, clientContext: { source: \"slack\", slack: {...} }, headers: x-open-agents-chat-id/session-id/user-id }). It persists the cursor immediately after send with SessionState { ...initialSession, sessionId: response.sessionId, streamIndex: firstStreamIndex, continuationToken? }, then iterates response events and inserts each into eve_chat_events with stream_index. After stream end it persists clientSession.state. Completion status is derived from the current-turn boundary event: session.waiting => waiting, session.failed => failed, else completed. Text response comes from latest message.completed whose finishReason !== \"tool-calls\".\n\nDB-backed profile resolution: agent/lib/open-agents-profile.ts defines OPEN_AGENTS_PROFILE_TOOLS = todo, read_file, write_file, grep, glob, bash, web_fetch, load_skill. resolveOpenAgentsProfile(ctx) gets userId from ctx.session.auth.initiator ?? current, using subject ?? principalId; gets sessionId from auth attributes.openAgentsSessionId; gets optional header tool profile from attributes.openAgentsToolProfile as comma-separated allowlist filtered to OPEN_AGENTS_PROFILE_TOOLS. If sessionId exists it reads sessions(id, user_id, agent_name, workspace_repos), returning null if row missing or user mismatch. If sessions.agent_name exists it reads agent_library_items for kind='agent' and item_id=agentName, searching scopes in order user/userId then org/OPEN_AGENTS_ORG_SCOPE_ID or default. Agent JSON shape expected by this file: { slug, name, description, tools, skills, systemPrompt }. Skill JSON shape expected: { id, name, description, body }. Skill resolution lists agent_library_items kind='skill' across org then user (reverse order so user overrides in Map), filters by agent.skills patterns supporting exact, prefix*, path/*, dotted.* against skill.id, skill.name, last path segment, and returns { id, name, description, markdown: body }. Return profile contains optional agentName, agentDisplayName, customInstructions, tools = header profile if present else agent.tools or default all tools, skills, workspaceRepos.\n\nSchema anchors from apps/open-agents/lib/db/schema.ts: slack_user_links columns id, user_id, slack_team_id, slack_user_id, slack_user_name, created_at, updated_at; unique indexes slack_user_links_user_idx(user_id), slack_user_links_identity_idx(slack_team_id, slack_user_id). slack_thread_sessions columns slack_team_id, slack_channel_id, slack_thread_ts, user_id, session_id, chat_id, link_posted_at, created_at, updated_at; primary key is slack_team_id/slack_channel_id/slack_thread_ts; unique slack_thread_sessions_chat_idx(chat_id). sessions columns include id, user_id, title, status enum running/completed/failed/archived, repo_owner, repo_name, branch, clone_url, workspace_repos jsonb WorkspaceRepo[] default [], global_skill_refs, agent_name, sandbox_state, lifecycle_state enum provisioning/active/hibernating/hibernated/restoring/archived/failed, lifecycle fields, timestamps. chats columns id, session_id, title, model_id default anthropic/claude-haiku-4.5, last_assistant_message_at, timestamps. eve_chat_session_states columns chat_id primary key, state jsonb SessionState, timestamps. eve_chat_events columns chat_id, stream_index, event_type, event jsonb HandleMessageStreamEvent, created_at; primary key chat_id/stream_index. agent_library_items columns id, user_id, scope_kind enum user/org, scope_id, kind enum agent/skill, item_id, item_json jsonb, timestamps; unique index on scope_kind/scope_id/kind/item_id.\n\nDynamic capabilities in this repo: agent/tools/open_agents_profile.ts imports defineDynamic + defineTool from eve/tools. It has one handler events[\"session.started\"] that calls resolveOpenAgentsProfile(ctx), maps profile.tools to DEFAULT_TOOLS, and returns Object.fromEntries([toolName, defineTool(DEFAULT_TOOLS[toolName])]). DEFAULT_TOOLS maps bash/glob/grep/read_file/write_file through OPEN_AGENTS_SESSION_TOOLS; todo/web_fetch/load_skill from eve/tools/defaults. agent/instructions/open_agents_profile.ts imports defineDynamic + defineInstructions from eve/instructions. Its session.started handler injects sections describing tool profile, sandbox usage, a prohibition on calling web app/Vercel SSO/api/eve/executor for workspace inspection, workspace repo clone locations, selected agent name/display name, and customInstructions/systemPrompt. Dynamic profile changes are session-start scoped in current code; a DB agent edit needs a new Eve session for normal profile reload.\n\nEve dynamic API evidence: node_modules/eve/dist/src/public/definitions/tool.d.ts defineDynamic({ events }): DynamicSentinel. Its docs in the d.ts say slot directory decides return shape: agent/tools returns defineTool or Record<string, defineTool> or null; agent/skills returns defineSkill or map or null; agent/instructions returns a single defineInstructions or null. It also states tool resolvers run at session.started, turn.started, step.started; instructions/skills only at session.started and turn.started. node_modules/eve/dist/src/shared/dynamic-tool-definition.d.ts defines DynamicResolveContext with session.id, session.auth, channel.kind/continuationToken/metadata, and visible messages.\n\nEve client/session APIs used by Slack and UI: node_modules/eve/dist/src/client/client.d.ts Client(options) exposes health(), info(), fetch(path, init?), session(state?: SessionState | string): ClientSession. node_modules/eve/dist/src/client/session.d.ts ClientSession has state getter, send<TOutput>(input: SendTurnInput<TOutput>): Promise<MessageResponse<TOutput>>, and stream(options?): AsyncIterable<HandleMessageStreamEvent>. Client docs explicitly say one Client can create many concurrent ClientSession handles, each tracking its own conversation state.\n\nMulti-agent primitives available in installed Eve:\n- One Eve deployment can host a root agent plus multiple declared local subagents under agent/subagents/<id>/, and nested subagents. Evidence: node_modules/eve/README.md authored directory lists agent/subagents/ as \"specialist child agents (reuse defineAgent)\"; node_modules/eve/docs/reference/project-layout.md says path agent/subagents/researcher/agent.ts resolves to subagent researcher, subagents/ is a slot, nested subagents are supported, channels/schedules are root-only, and subagent agent.ts must declare description. This is not multiple independent channel-addressable root agents; identity of root comes from package/app root and subagents are model-visible tools.\n- Built-in sub-agent primitive: node_modules/eve/docs/subagents.mdx says every agent gets built-in `agent` tool by default, input shape { message: string; outputSchema?: object }, delegating to a copy of itself. Copy shares parent sandbox/tools, inherits auth/connections, fresh history/state. Multiple `agent` calls in one model response run concurrently and return all results before parent continues. An authored tool at agent/tools/agent.ts overrides the built-in.\n- Declared local subagents: agent/subagents/<id>/ with defineAgent({ description, model?, ... }). The parent sees a tool named by bare directory id, e.g. researcher, with the same { message, outputSchema? } input. Declared subagents inherit nothing from root authored slots: own instructions/tools/connections/skills/sandbox/hooks/subagents; channels and schedules root-only; default subagent depth cap is 3 unless defineAgent({ limits: { maxSubagentDepth } }). Tool-name collisions with authored tools reject build. Parent stream has subagent.called and subagent.completed; child stream is available via subagent.called.data.childSessionId at GET /eve/v1/session/:childSessionId/stream.\n- Remote agent-to-agent calls: node_modules/eve/docs/guides/remote-agents.md and node_modules/eve/dist/src/public/definitions/remote-agent.d.ts show defineRemoteAgent(input: RemoteAgentDefinitionInput): RemoteAgentDefinition. Authored under agent/subagents/<id>. RemoteAgentDefinition fields: url required, description required, optional auth OutboundAuthFn, headers, outputSchema, path default /eve/v1/session, kind: \"remote\". The compiler lowers it to a subagent tool. Remote dispatch starts task-mode POST /eve/v1/session on another Eve deployment with a callback, parks parent durably until terminal callback, then surfaces result as subagent tool result. Runtime action types include RuntimeRemoteAgentCallActionRequest kind \"remote-agent-call\" with callId, description, input, name, nodeId, remoteAgentName.\n- Runtime subagent internals: node_modules/eve/dist/src/runtime/actions/types.d.ts defines RuntimeSubagentCallActionRequest kind \"subagent-call\" with callId, description, input JsonObject, name, nodeId, subagentName; RuntimeSubagentResultActionResult kind \"subagent-result\" with callId, output, subagentName, isError?. node_modules/eve/dist/src/channel/types.d.ts defines SessionParent { callId, rootSessionId, sessionId, turn }, SessionCallback { callId, subagentName, token, url }, RunInput.parent?, RunInput.subagentDepth?, RunInput.subagentMaxDepth?, and comments that subagent dispatch inherits parent capabilities and initiatorAuth.\n- Handoffs: installed Eve uses the term for cross-channel/session delivery, not a first-class `defineHandoff` agent-delegation API found in public exports. Evidence: rg of public d.ts/docs found handoff references in channel docs/types such as defineChannel accepting cross-channel handoffs and Slack thread context for cross-channel handoffs, plus scheduling docs saying channel handoff can wait for Slack reply. No public `defineHandoff` type was found. For agent-to-agent delegation, use subagent/remote-agent tools.\n- Routers/multiple agent slots: no public `defineRouter` or router type was found in node_modules/eve/dist/src/public or docs. Routing can be built at the channel/app layer or by giving the model multiple subagent tools, but installed Eve’s verified multi-agent public primitives are root agent + subagents/remote agents lowered as tools.\n\nExplicit answers: Can one Eve deployment host multiple named agents? Yes for multiple named subagents in one root deployment via agent/subagents/<id>/, including nested subagents and remote-agent declarations; the root deployment is still a single root agent for channels. Cite project-layout path-derived naming and subagents docs; type signature for subagent configs is the same defineAgent<TAgent extends AgentDefinition>(...): TAgent and remote agents use defineRemoteAgent(input): RemoteAgentDefinition. Can an agent invoke another agent as a tool or handoff? Yes as a tool: built-in `agent`, declared local subagents, and defineRemoteAgent all lower to model-visible tools with { message, outputSchema? }. Runtime action signatures prove this with RuntimeSubagentCallActionRequest kind \"subagent-call\" and RuntimeRemoteAgentCallActionRequest kind \"remote-agent-call\". Not verified as a first-class handoff API; use remote subagent for another deployment.\n\nArchitectural gotchas: Current DB-backed agent profiles are dynamic prompts/tools layered onto a single Eve root agent; they are not Eve subagents and do not produce multiple Eve agent slots. Header x-open-agents-tool-profile can override DB tools but only to the fixed OPEN_AGENTS_PROFILE_TOOLS allowlist. Slack-created Eve sessions rely on immediate cursor persistence before streamed events; do not wait for response.result() or only persist after completion. Eve subagent local declared specialists do not inherit root Open Agents dynamic tools/instructions unless duplicated or factored into their own subagent authored slots; built-in `agent` copies do inherit the current agent’s tools/sandbox. Subagent names share the tool namespace and can collide with authored tools. Channels are root-only, so Slack/Eve HTTP ingress cannot directly target local declared subagents as separate agents without custom routing/delegation.\n\nGAPS: I did not run `eve info` or inspect compiled .eve artifacts, so this briefing verifies source and installed package types/docs, not current compiler output. I did not find any existing agent/subagents/* directories in this repo; multi-agent support is from installed Eve package docs/types. I could not verify a public `defineHandoff` or `defineRouter` API; searches only found channel-level handoff mentions and no router export. I did not inspect every Eve internal d.ts in full; pinned files cover the public and runtime signatures relevant to agent definition, dynamic capabilities, sessions, subagent/remote-agent runtime actions, and channel lineage."
}
```
