```json
{
  "pins": [
    "docs/open-agents-operations-handoff.md",
    "apps/open-agents/app/sessions/[sessionId]/chats/[chatId]/page.tsx",
    "apps/open-agents/app/sessions/[sessionId]/chats/[chatId]/hooks/use-session-chat-runtime.ts",
    "apps/open-agents/app/sessions/[sessionId]/chats/[chatId]/session-chat-context.tsx",
    "apps/open-agents/app/sessions/[sessionId]/chats/[chatId]/session-chat-content.tsx",
    "apps/open-agents/app/api/sessions/_lib/session-context.ts",
    "apps/open-agents/lib/session/get-server-session.ts",
    "apps/open-agents/lib/session/server.ts",
    "apps/open-agents/lib/auth/config.ts",
    "apps/open-agents/lib/db/schema.ts",
    "apps/open-agents/lib/db/sessions.ts",
    "apps/open-agents/lib/db/eve-chat-sessions.ts",
    "apps/open-agents/app/api/sessions/[sessionId]/chats/[chatId]/eve/route.ts",
    "apps/open-agents/app/api/sessions/[sessionId]/chats/[chatId]/share/route.ts",
    "apps/open-agents/app/api/sandbox/route.ts",
    "apps/open-agents/app/api/sandbox/status/route.ts",
    "apps/open-agents/app/api/sandbox/reconnect/route.ts",
    "apps/open-agents/app/api/sandbox/pause/route.ts",
    "apps/open-agents/app/api/sandbox/resume/route.ts",
    "apps/open-agents/app/api/sandbox/activity/route.ts",
    "apps/open-agents/app/api/sandbox/extend/route.ts",
    "apps/open-agents/app/api/settings/agent-library/route.ts",
    "apps/open-agents/lib/agents/repository.ts",
    "apps/open-agents/app/settings/agents-section.tsx",
    "apps/open-agents/app/shared/[shareId]/page.tsx",
    "apps/open-agents/app/api/shared/[shareId]/markdown/route.ts"
  ],
  "briefing": "Orientation: read docs/open-agents-operations-handoff.md first. It says apps/open-agents is the Next.js UI/auth/session DB/sandbox API/Eve chat persistence surface, with key tables sessions, chats, eve_chat_session_states, eve_chat_events, shares, slack_user_links, slack_thread_sessions, and agent_library_items.\n\nRoute tree: top-level UI routes include /, /deploy-your-own, /get-started, /sessions, /sessions/automations, /sessions/[sessionId], /sessions/[sessionId]/chats/[chatId], /sessions/[sessionId]/executor/[[...path]], /codespace/[sessionId], /settings/*, /shared/[shareId], /[username], /[username]/[repo], /u/[username]. API routes include auth, sessions/chat/share/eve/read/diff/files/skills/checks/code-editor/dev-server, sandbox, settings, github, vercel, automations, shared markdown/status, executor, health, models, usage, source-polling, transcribe, webhooks/linear. A deeper find showed additional nested routes missed by shallow scans: /api/sessions/[sessionId]/chats/[chatId]/share, /read, /checks/fix, /diff/cached, /diff/patch, /files/content, /api/vercel/projects/[idOrName]/env.\n\nPinned session page: apps/open-agents/app/sessions/[sessionId]/chats/[chatId]/page.tsx. It server-auths with getServerSession, redirects unauthenticated users, loads sessions via getSessionByIdCached, and enforces ownership by sessionRecord.userId !== session.user.id redirecting to / (lines 97-116). It fetches chat, Eve snapshot, model/options, preferences, and chat summaries in parallel (lines 120-134), converts Eve events to WebAgent messages, sanitizes model/preferences, then wraps SessionChatContent with SessionChatProvider (lines 143-190). Important invariant: it calls getEveChatSessionSnapshot(chatId) before checking chat existence beyond ownership; ownership of the session is already checked, but chatId/sessionId matching is checked after the parallel fetch.\n\nChat UI composition: SessionChatProvider in session-chat-context.tsx owns client state for session, chatInfo, sandboxInfo cache, model options/preferences, Eve runtime, diff/git/files/skills SWR hooks, archive/unarchive, title/model mutation, sandbox reconnect/status sync, PR metadata. SessionChatContent is the large UI surface: dynamic DiffViewer, FileTabView, GitPanel, ShareDialog, sandbox banners/actions, input overlay, message rendering, tool cards, PR/repo dialogs. ShareDialog lives in session-chat-content.tsx around lines 788-993.\n\nuse-session-chat-runtime flow: apps/open-agents/app/sessions/[sessionId]/chats/[chatId]/hooks/use-session-chat-runtime.ts creates an eve/client Client with host: '' and preserveCompletedSessions, seeded from initialEveSession (lines 96-100). It hydrates useEveAgent with initialEvents and the session (lines 154-170). It tracks nextEveStreamIndexRef initialized from initialEveEvents.length (lines 92-94, 111-113). The persistence endpoint is /api/sessions/:sessionId/chats/:chatId/eve (lines 115-119). enqueueEvePersistence chains onto persistenceQueueRef.current, swallowing prior rejection and POSTing JSON sequentially; failures are logged asynchronously (lines 121-140). onEvent persists each event immediately with firstStreamIndex and increments the local stream index (lines 157-160). onSessionChange and onFinish persist SessionState/cursor (lines 163-168). sendMessage converts UI input via toEveSendPayload and wraps client context {sessionId, chatId, contextLimit} before agent.send (lines 144-150, 203-206). Tool approvals/input responses are sent as Eve inputResponses (lines 210-229). setMessages resets Eve projection and replaces persistedMessages (lines 189-195).\n\nPersistence tables/types: schema.ts defines sessions.user_id, chats.session_id, eve_chat_session_states.chat_id/state, eve_chat_events.chat_id/stream_index/event_type/event with PK(chat_id, stream_index), shares.chat_id unique, chat_reads PK(user_id, chat_id) (lines 119-259). agent_library_items has id, user_id, scope_kind enum user|org, scope_id, kind enum agent|skill, item_id, item_json, unique(scope_kind, scope_id, kind, item_id) (lines 684-715). user_preferences holds default_agent_name, global_skill_refs, model_variants, enabled_model_ids, public_usage_enabled, default model/sandbox/diff/auto flags (lines 721-746).\n\nEve persistence API: app/api/sessions/[sessionId]/chats/[chatId]/eve/route.ts requires authenticated user then requireOwnedSessionChat({userId, sessionId, chatId}) before accepting writes (lines 89-103). Request body requires events plus firstStreamIndex when events exist, and/or session state with streamIndex (lines 13-44). It logs Eve runtime failure events and calls persistEveChatSessionPatch; append uses onConflictDoNothing and updates chats.updated_at/last_assistant_message_at when message.completed appears in lib/db/eve-chat-sessions.ts.\n\nMain auth helper: apps/open-agents/lib/session/get-server-session.ts and lib/session/server.ts are the auth wrappers. Both use local development session when OPEN_AGENTS_AUTH_MODE local is enabled; otherwise they import auth from lib/auth/config and call better-auth api.getSession with request/Next headers. Returned Session.user.id is Better Auth users.id. Better Auth config is in lib/auth/config.ts using drizzleAdapter(authDb), users/auth_sessions/account/verification schema, Vercel and GitHub social providers, trusted account linking, encrypted OAuth tokens, and username derivation.\n\nAuth gates: there is no checked-in apps/open-agents/middleware.ts or middleware source under apps/open-agents; only .next build artifacts exist. Page-level gates: app/sessions/layout.tsx redirects unauthenticated users and preloads active sessions/lastRepo; app/settings/layout.tsx redirects via getSessionUserInfo. Session chat page additionally enforces sessions.user_id equality. API-level gates are per-route.\n\nOwnership helper: app/api/sessions/_lib/session-context.ts is the canonical route helper. requireAuthenticatedUser returns 401 when getServerSession has no user (lines 65-78). requireOwnedSession loads sessions by id and checks sessionRecord.userId !== userId for 403 (lines 80-103). requireOwnedSessionWithSandboxGuard adds a sandboxState predicate (lines 106-134). requireOwnedSessionChat loads session and chat in parallel, checks session userId equality, then checks chat.sessionId === sessionId (lines 137-170).\n\nSession API ownership map: /api/sessions GET lists only getSessionsWithUnreadByUserId(session.user.id); POST creates sessions with userId=session.user.id. /api/sessions/[sessionId] GET/PATCH/DELETE manually checks existingSession.userId === session.user.id. /chats GET/POST uses requireOwnedSession. /chats/[chatId] GET/PATCH/DELETE uses requireOwnedSessionChat. /chats/[chatId]/eve uses requireOwnedSessionChat. /chats/[chatId]/share uses requireOwnedSessionChat before GET/POST/DELETE. /chats/[chatId]/read uses requireOwnedSessionChat. /diff, /diff/patch, /files, /files/content, /code-editor, /dev-server use requireOwnedSessionWithSandboxGuard. /skills, /checks/fix, /diff/cached use requireOwnedSession. /generate-commit-message manually checks dbSession.userId === session.user.id.\n\nSandbox API routes: /api/sandbox POST creates/resumes a named persistent Vercel sandbox after getServerSession, BotID, rate limit, and requireOwnedSession; it verifies GitHub repo access before minting setup tokens, connects with source/sources derived from session cloneUrl/workspaceRepos, persists sandboxState and lifecycle active fields, installs global skills and agent local skills, and kicks sandbox lifecycle. DELETE requires auth, BotID/rate limit, requireOwnedSession, then stops sandbox if canOperateOnSandbox and clears state. /status GET requires auth + requireOwnedSession and returns active/no_sandbox from DB lifecycle/runtime state. /reconnect GET requires auth + requireOwnedSession, probes persisted runtime sandbox with connectSandbox + pwd, refreshes sandboxState/expiresAt, or clears unavailable state. /pause POST requires auth + requireOwnedSessionWithSandboxGuard(canOperateOnSandbox), stops sandbox, clears runtime state, writes hibernated lifecycle. /resume PUT requires auth + requireOwnedSession, requires sandboxState.type vercel, resumes by persisted sandboxName, writes active lifecycle, clears resume state if sandbox missing. /activity POST requires auth + requireOwnedSession and updates lastActivityAt/hibernateAfter only when lifecycleState active. /extend POST requires auth + BotID/rate limit + requireOwnedSessionWithSandboxGuard(isSandboxActive), calls sandbox.extendTimeout, persists new state/lifecycle.\n\nSharing/visibility/permissions UI: shares is the only explicit public sharing table in this surface. /api/sessions/:sessionId/chats/:chatId/share requires requireOwnedSessionChat for GET existing share, POST create nanoid(12) share, DELETE revoke. Public /shared/[shareId] page does not require auth; it resolves shares -> chats -> sessions, fetches public owner profile fields users.username/name/avatarUrl, redacts shared env content, shows ownerSessionHref only if viewerSession.user.id === session.userId. /api/shared/[shareId]/markdown is public and exports redacted text/markdown; it includes YAML-ish frontmatter and emits HTML comments like <!-- tool_activity: ... --> for hidden tool/reasoning activity. /api/shared/[shareId]/status is public read-only and returns streaming status by shareId. There is no verified general comments feature in the focused files; “comments” found here are markdown export annotations/code comments, not user comments.\n\nAgent/skill/tool library UI: /settings/agents renders AgentsSection. It lists agents/skills, has AgentEditor and SkillEditor, SaveScopeSelect for user/org scope, Default badge/action, delete/new/save flows. API /api/settings/agent-library requires getServerSession for all verbs. GET returns listAgentLibrary(preferences.defaultAgentName, user.id). POST validates agentEditorInputSchema or skillEditorInputSchema and saves via saveAgentDefinition/saveSkillDocument with scope user|org; setDefault updates user_preferences.default_agent_name. PATCH validates and updates defaultAgentName after getAgentDefinition(defaultAgentName, user.id). DELETE deletes agent/skill by id/scope; deleting the default agent clears preference. Repository layer lib/agents/repository.ts merges bundled markdown files under .agents/agents and .agents/skills with DB-backed user/org rows from agent_library_items. Read precedence: databaseScopesForRead returns org then user; later entries override in maps, and getAgentDefinition/getSkillDocument reverse scopes so user wins over org. Save with userId always writes DB rows; without userId writes markdown files.\n\nMarkdown/doc editing: agent/skill library editing is markdown-derived but DB-backed in app usage: bundled agents/skills are parsed from markdown with YAML frontmatter; user/org saves persist structured editor JSON to agent_library_items.item_json and db: paths. saveAgentDefinition/saveSkillDocument without userId can write actual .agents markdown, but the API always passes user.id. Shared markdown export is read-only public generation, not editing. I did not verify any collaborative doc editor or comments product beyond agent/skill textareas and shared markdown export.\n\nOther API auth notes: /api/executor/[[...path]] requires getServerSession; if path starts session/:id it also checks sessionRecord.userId === authSession.user.id before proxying executor runtime. /api/eve/executor/execute does not use Better Auth; it trusts x-open-agents-user-id unless x-open-agents-authenticator is slack-webhook, in which case it maps Slack team/user to slack_user_links.user_id. /api/health is public, including ?db=1 connection snapshot. /api/source-polling is authorized only by CRON_SECRET bearer when CRON_SECRET is set; otherwise public. /api/github/webhook verifies GitHub webhook signature, not user session, and maps installation rows to user scopes for automation events. GitHub user-facing routes generally require getServerSession and operate with session.user.id tokens/installations. /api/vercel/projects/[idOrName]/env currently always returns 404. /api/vercel/repo-projects requires getServerSession and user Vercel token. /api/generate-pr checks sessionRecord.userId equality; /generate-title and /transcribe require auth/rate limit but no session ownership object. /api/models uses getServerSession for filtering. /api/usage and /usage/rank require auth; rank filters the current user's row specially.\n\nRefactor gotchas: preserve the queued persistence semantics in use-session-chat-runtime; events and SessionState are intentionally persisted as the Eve stream advances, not after final result. Preserve stream_index monotonicity and PK(chat_id, stream_index). Public share routes/pages intentionally bypass auth after shareId lookup; owner-only operations are create/revoke via nested chat share route. Most session/sandbox ownership is userId equality on sessions.user_id; no role/visibility ACL exists in focused source. Org-scoped agent library items are not authorization-gated beyond any authenticated user being allowed to save/delete with scope='org' through the current API.\n\nGAPS: I did not read every large TSX line in session-chat-content.tsx verbatim beyond symbol inventory and ShareDialog location; workers changing UI should re-read the pinned file. I did not deeply inspect all automations route policies/store ownership; only auth grep showed they use getServerSession/userId in several routes while scheduler/source polling are service-style. I did not verify Linear webhook auth because focus was open-agents app/lib/middleware and sandbox/session surfaces. I found no checked-in Open Agents middleware source; only .next build artifacts, so middleware behavior should be treated as absent unless generated by Next/plugins at build time. I found no concrete user comments feature in the focused files."
}
```
