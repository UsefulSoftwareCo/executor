diff --git a/lib/Apply.js b/lib/Apply.js
index c099d206676c04d44aa68d30f1178c31ce1a03f4..cad6f5823d2f7d3dba7239d8c19f4b63c8d4fb7e 100644
--- a/lib/Apply.js
+++ b/lib/Apply.js
@@ -391,13 +391,17 @@ const executeNode = (fqn, node, tracker, ready, readyStable, terminalStatuses, s
         if (policyChanged && node.state.removalPolicy !== undefined) {
             yield* scopedSession.note(`removal policy ${node.state.removalPolicy} → ${node.resource.RemovalPolicy}`);
         }
-        yield* signalReadyStable;
+        // Store before signalling. A dependent task released by
+        // `readyStable` reads `tracker[fqn]`, so signalling first lets it
+        // observe a missing output. Every other lifecycle path stores first.
+        // See https://github.com/alchemy-run/alchemy/issues/1717.
         yield* storeAndSignal({
             output: node.state.attr,
             props: node.state.props,
             bindings: node.state.bindings ?? [],
             instanceId: node.state.instanceId,
         });
+        yield* signalReadyStable;
         return;
     }
     const allUpstreamFqns = () => {
diff --git a/lib/Cloudflare/Hyperdrive/Connection.js b/lib/Cloudflare/Hyperdrive/Connection.js
index ad4516d4ee17ed6dc1ef87db51f5f6c49fbc3f7b..a177d5327a1ff58facea17c9d74264de09a7c444 100644
--- a/lib/Cloudflare/Hyperdrive/Connection.js
+++ b/lib/Cloudflare/Hyperdrive/Connection.js
@@ -194,7 +194,7 @@ export const ProviderLive = () => Provider.succeed(Connection, {
 export const ProviderLocal = () => Provider.succeed(Connection, {
     stables: ["accountId"],
     diff: Effect.fn(function* ({ news, output }) {
-        const { accountId } = yield* yield* CloudflareEnvironment;
+        const accountId = output?.accountId ?? "local";
         if (!output?.hyperdriveId)
             return { action: "update" };
         if (!isResolved(news))
@@ -210,7 +210,7 @@ export const ProviderLocal = () => Provider.succeed(Connection, {
         return output ?? undefined;
     }),
     reconcile: Effect.fn(function* ({ id, news, output }) {
-        const { accountId } = yield* yield* CloudflareEnvironment;
+        const accountId = output?.accountId ?? "local";
         return {
             hyperdriveId: output?.hyperdriveId ?? generateLocalId(),
             name: yield* createConfigName(id, news.name),
diff --git a/lib/Cloudflare/R2/Bucket.js b/lib/Cloudflare/R2/Bucket.js
index a87d74325befa35f68b297f1dd7ef0fbafe5362f..959ef148b7f37baabf2abb9beda7a71bd83f075d 100644
--- a/lib/Cloudflare/R2/Bucket.js
+++ b/lib/Cloudflare/R2/Bucket.js
@@ -858,7 +858,7 @@ export const ProviderLive = () => Provider.effect(Bucket, Effect.gen(function* (
 export const ProviderLocal = () => Provider.succeed(Bucket, {
     stables: ["accountId"],
     diff: Effect.fn(function* ({ news = {}, output }) {
-        const { accountId } = yield* yield* CloudflareEnvironment;
+        const accountId = output?.accountId ?? "local";
         if (!output?.bucketName)
             return { action: "update" };
         if (!isResolved(news))
@@ -873,7 +873,7 @@ export const ProviderLocal = () => Provider.succeed(Bucket, {
         return output ?? undefined;
     }),
     reconcile: Effect.fn(function* ({ news = {}, output }) {
-        const { accountId } = yield* yield* CloudflareEnvironment;
+        const accountId = output?.accountId ?? "local";
         return {
             bucketName: output?.bucketName ?? generateLocalId(),
             storageClass: (news.storageClass ?? "Standard"),
diff --git a/lib/Cloudflare/Workers/DurableObjectBridge.js b/lib/Cloudflare/Workers/DurableObjectBridge.js
index d5e365c27c703aa143792cd6c786676a6a90c9de..724fec4eb9d6f7207805738c5459f60628baa694 100644
--- a/lib/Cloudflare/Workers/DurableObjectBridge.js
+++ b/lib/Cloudflare/Workers/DurableObjectBridge.js
@@ -33,10 +33,11 @@ export const makeDurableObjectBridge = (DurableObject, { entrypoint, stack, }) =
         constructor(state, env) {
             super(state, env);
             this.#state = state;
+            const instanceScope = Scope.makeUnsafe();
             this.#instance = state.blockConcurrencyWhile(() => build((promise) => void state.waitUntil?.(promise)).then(({ context, export: exported, telemetry }) => {
                 const { constructor, services } = exported;
-                const doContext = Layer.succeed(DurableObjectState, fromDurableObjectState(this.#state)).pipe(Layer.provideMerge(Layer.succeedContext(services)), Layer.provideMerge(Layer.succeedContext(context)));
-                return constructor.pipe(Effect.provide(doContext), Effect.flatMap((instance) => instance.pipe(Effect.provide(doContext))), Effect.map((instance) => ({
+                const doContext = Layer.mergeAll(Layer.succeed(DurableObjectState, fromDurableObjectState(this.#state)), Layer.succeed(Scope.Scope, instanceScope)).pipe(Layer.provideMerge(Layer.succeedContext(services)), Layer.provideMerge(Layer.succeedContext(context)));
+                return constructor.pipe(Effect.provide(doContext), Effect.flatMap((instance) => instance.pipe(Effect.provide(doContext))), Effect.onExit((exit) => Exit.isFailure(exit) ? Scope.close(instanceScope, exit) : Effect.void), Effect.map((instance) => ({
                     instance,
                     services,
                     context,
diff --git a/lib/Cloudflare/Workers/HttpServer.js b/lib/Cloudflare/Workers/HttpServer.js
index 53d2536b6c086fb167269e07f1c04a1a2d1f55b6..80628efc856170ebaea904b32e95758018cd294e 100644
--- a/lib/Cloudflare/Workers/HttpServer.js
+++ b/lib/Cloudflare/Workers/HttpServer.js
@@ -1,7 +1,9 @@
 import * as Deferred from "effect/Deferred";
 import * as Effect from "effect/Effect";
-import * as Layer from "effect/Layer";
 import * as Option from "effect/Option";
+import * as Scope from "effect/Scope";
+import * as Stream from "effect/Stream";
+import * as HttpBody from "effect/unstable/http/HttpBody";
 import * as EffectHttp from "effect/unstable/http/HttpEffect";
 import * as HttpServerRequest from "effect/unstable/http/HttpServerRequest";
 import * as HttpServerResponse from "effect/unstable/http/HttpServerResponse";
@@ -22,23 +24,30 @@ export const makeRequestEffect = (webRequest, handler) => {
                 raw: webRequest.body,
             }),
         });
-        return yield* toHandledWebResponse(safeHandler).pipe(Effect.provide([
-            Layer.succeed(HttpServerRequest.HttpServerRequest, request),
-            Layer.succeed(Request, webRequest),
-        ]));
+        return yield* toHandledWebResponse(safeHandler).pipe(Effect.provideService(HttpServerRequest.HttpServerRequest, request), Effect.provideService(Request, webRequest));
     });
 };
 const toHandledWebResponse = (handler) => Effect.gen(function* () {
     // `toHandled` exposes the final response through this callback, not its
     // return value. Keep the assignment isolated here so callers get Response.
     const context = yield* Effect.context();
+    const eventScope = yield* Scope.Scope;
     const webResponse = yield* Deferred.make();
+    const toWeb = (request, response) => {
+        let transferred = request.method === "HEAD" ? response : EffectHttp.scopeTransferToStream(response);
+        const streaming = request.method !== "HEAD" && transferred.body instanceof HttpBody.Stream;
+        if (streaming && transferred.body instanceof HttpBody.Stream) {
+            const body = transferred.body;
+            transferred = HttpServerResponse.setBodyKeepHeaders(transferred, HttpBody.stream(
+                body.stream.pipe(Stream.onExit((exit) => Scope.close(eventScope, exit))),
+                body.contentType, body.contentLength));
+        }
+        const web = HttpServerResponse.toWeb(transferred, { withoutBody: request.method === "HEAD", context });
+        if (streaming) EffectHttp.scopeDisableClose(eventScope);
+        return web;
+    };
     yield* EffectHttp.toHandled(handler, (request, response) => Deferred.succeed(webResponse, 
-    // Conversion to web response with options matches `EffectHttp.toWebHandler`'s callback.
-    HttpServerResponse.toWeb(EffectHttp.scopeTransferToStream(response), {
-        withoutBody: request.method === "HEAD",
-        context,
-    })));
+    toWeb(request, response))).pipe(Effect.forkScoped);
     return yield* Deferred.await(webResponse);
 });
 export { isScopeEjected } from "../../Http.js";
diff --git a/lib/Cloudflare/Workers/LocalWorkerProvider.js b/lib/Cloudflare/Workers/LocalWorkerProvider.js
index 3eefe4f3d6a3d397fe64ced4fb1107aefaa687b2..b27c9684f6ced28d13c5ae524d077579565617d7 100644
--- a/lib/Cloudflare/Workers/LocalWorkerProvider.js
+++ b/lib/Cloudflare/Workers/LocalWorkerProvider.js
@@ -455,7 +455,7 @@ export const LocalWorkerProvider = () => LocalProvider.make(Worker, LOCAL_PROVID
      * half of the old `buildConfig` — kept out of the hashed config.
      */
     const materializeWorkerBindings = Effect.fn(function* (config, selfUrl) {
-        const { accountId } = yield* cloudflareEnv;
+        const { accountId } = yield* (Object.values(config.devRemote).some(Boolean) ? cloudflareEnv : Effect.succeed({ accountId: "local" }));
         return yield* materializeRuntimeBindings({
             ...config,
             devAccess: config.dev.mode === "external" ? undefined : config.dev.access,
@@ -927,7 +927,7 @@ export const LocalWorkerProvider = () => LocalProvider.make(Worker, LOCAL_PROVID
         // The dev server and its workerd run in a child process rooted
         // at the app.
         const root = path.resolve(rootDir ?? process.cwd());
-        const { accountId } = yield* cloudflareEnv;
+        const { accountId } = yield* (Object.values(worker.devRemote ?? {}).some(Boolean) ? cloudflareEnv : Effect.succeed({ accountId: "local" }));
         // Queue-consumer wiring can change while the child is starting
         // (a sibling `Consumer` reconcile), before the restart hook
         // below exists to pick it up. We hold the serve lock, so a
@@ -1076,14 +1076,17 @@ export const LocalWorkerProvider = () => LocalProvider.make(Worker, LOCAL_PROVID
             const durableObjectNamespaces = {};
             for (const { data } of bindings) {
                 for (const binding of data?.bindings ?? []) {
-                    if (binding.type === "durable_object_namespace") {
+                    // Foreign bindings may contain unresolved script-name Outputs during precreate.
+                    // Their namespace belongs to the host Worker and must not be declared here.
+                    if (binding.type === "durable_object_namespace" &&
+                        (!binding.scriptName || binding.scriptName === name)) {
                         durableObjectNamespaces[binding.className] =
                             binding.namespaceId ??
                                 encodeURIComponent(`${binding.scriptName}-${binding.className}`);
                     }
                 }
             }
-            const { accountId } = yield* cloudflareEnv;
+            const { accountId } = yield* (bindings.some(({ data }) => Object.values(data?.devRemote ?? {}).some(Boolean)) ? cloudflareEnv : Effect.succeed({ accountId: "local" }));
             const urls = news.dev?.mode === "external"
                 ? // news.dev.url may be an unresolved output; avoid trying to resolve it here.
                     []
@@ -1111,7 +1114,7 @@ export const LocalWorkerProvider = () => LocalProvider.make(Worker, LOCAL_PROVID
             };
         }),
         start: Effect.fn(function* ({ fqn, config, invalidate }) {
-            const { accountId } = yield* cloudflareEnv;
+            const { accountId } = yield* (Object.values(config.devRemote).some(Boolean) ? cloudflareEnv : Effect.succeed({ accountId: "local" }));
             // `dev: { mode: "external" }` opts out of running a local Worker
             // entirely — typically because an external dev process
             // (Command.Dev) is serving requests. The instance exists in the
diff --git a/src/Apply.ts b/src/Apply.ts
index 4e056c4e52c2a6f9b4d8ed08bb35d900171c4d07..c11421fffcc230058dae2f2acde96a0a761418e1 100644
--- a/src/Apply.ts
+++ b/src/Apply.ts
@@ -747,13 +747,17 @@ const executeNode = (
           `removal policy ${node.state.removalPolicy} → ${node.resource.RemovalPolicy}`,
         );
       }
-      yield* signalReadyStable;
+      // Store before signalling. A dependent task released by
+      // `readyStable` reads `tracker[fqn]`, so signalling first lets it
+      // observe a missing output. Every other lifecycle path stores first.
+      // See https://github.com/alchemy-run/alchemy/issues/1717.
       yield* storeAndSignal({
         output: node.state.attr,
         props: node.state.props,
         bindings: node.state.bindings ?? [],
         instanceId: node.state.instanceId,
       });
+      yield* signalReadyStable;
       return;
     }
 
diff --git a/src/Cloudflare/Hyperdrive/Connection.ts b/src/Cloudflare/Hyperdrive/Connection.ts
index 9a0aa1a8c503d2f84b7ee7bc3f93549dc11f501c..786e62070efa2e2c3965faf69bb5fa00adf64d35 100644
--- a/src/Cloudflare/Hyperdrive/Connection.ts
+++ b/src/Cloudflare/Hyperdrive/Connection.ts
@@ -338,7 +338,7 @@ export const ProviderLocal = () =>
   Provider.succeed(Connection, {
     stables: ["accountId"],
     diff: Effect.fn(function* ({ news, output }) {
-      const { accountId } = yield* yield* CloudflareEnvironment;
+      const accountId = output?.accountId ?? "local";
       if (!output?.hyperdriveId) return { action: "update" } as const;
       if (!isResolved(news)) return undefined;
       if (output.accountId !== accountId) {
@@ -352,7 +352,7 @@ export const ProviderLocal = () =>
       return output ?? undefined;
     }),
     reconcile: Effect.fn(function* ({ id, news, output }) {
-      const { accountId } = yield* yield* CloudflareEnvironment;
+      const accountId = output?.accountId ?? "local";
       return {
         hyperdriveId: output?.hyperdriveId ?? generateLocalId(),
         name: yield* createConfigName(id, news.name),
diff --git a/src/Cloudflare/R2/Bucket.ts b/src/Cloudflare/R2/Bucket.ts
index a1f2df8a4ebbd5b31d500637ae30d5cd1688506f..2e33c9301b379e760a0804931c85f99b7628575b 100644
--- a/src/Cloudflare/R2/Bucket.ts
+++ b/src/Cloudflare/R2/Bucket.ts
@@ -1388,7 +1388,7 @@ export const ProviderLocal = () =>
   Provider.succeed(Bucket, {
     stables: ["accountId"],
     diff: Effect.fn(function* ({ news = {}, output }) {
-      const { accountId } = yield* yield* CloudflareEnvironment;
+      const accountId = output?.accountId ?? "local";
       if (!output?.bucketName) return { action: "update" } as const;
       if (!isResolved(news)) return undefined;
       if (output.accountId !== accountId) {
@@ -1401,7 +1401,7 @@ export const ProviderLocal = () =>
       return output ?? undefined;
     }),
     reconcile: Effect.fn(function* ({ news = {}, output }) {
-      const { accountId } = yield* yield* CloudflareEnvironment;
+      const accountId = output?.accountId ?? "local";
       return {
         bucketName: output?.bucketName ?? generateLocalId(),
         storageClass: (news.storageClass ?? "Standard") as Bucket.StorageClass,
diff --git a/src/Cloudflare/Workers/DurableObjectBridge.ts b/src/Cloudflare/Workers/DurableObjectBridge.ts
index 83cc61f5870ee6e74bede2d3396e28050d9b793c..37e603734a192ae9351c459dda1b6358c4e23e32 100644
--- a/src/Cloudflare/Workers/DurableObjectBridge.ts
+++ b/src/Cloudflare/Workers/DurableObjectBridge.ts
@@ -60,14 +60,17 @@ export const makeDurableObjectBridge =
       constructor(state: cf.DurableObjectState, env: any) {
         super(state as any, env);
         this.#state = state;
+        // Constructor resources belong to the object, not a transient Layer.provide scope.
+        // workerd has no teardown hook; failed construction must still release them.
+        const instanceScope = Scope.makeUnsafe();
 
         this.#instance = state.blockConcurrencyWhile(() =>
           build((promise) => void (state as any).waitUntil?.(promise)).then(
             ({ context, export: exported, telemetry }) => {
               const { constructor, services } = exported;
-              const doContext = Layer.succeed(
-                DurableObjectState,
-                fromDurableObjectState(this.#state),
+              const doContext = Layer.mergeAll(
+                Layer.succeed(DurableObjectState, fromDurableObjectState(this.#state)),
+                Layer.succeed(Scope.Scope, instanceScope),
               ).pipe(
                 Layer.provideMerge(Layer.succeedContext(services)),
                 Layer.provideMerge(Layer.succeedContext(context)),
@@ -77,6 +80,9 @@ export const makeDurableObjectBridge =
                 Effect.flatMap((instance) =>
                   instance.pipe(Effect.provide(doContext)),
                 ),
+                Effect.onExit((exit) =>
+                  Exit.isFailure(exit) ? Scope.close(instanceScope, exit) : Effect.void,
+                ),
                 Effect.map((instance) => ({
                   instance,
                   services,
diff --git a/src/Cloudflare/Workers/HttpServer.ts b/src/Cloudflare/Workers/HttpServer.ts
index 73034de2da062d5b083e213aeb4a087ae7fa2989..54a73a37a0268d1236af0dcd345fec76b165371b 100644
--- a/src/Cloudflare/Workers/HttpServer.ts
+++ b/src/Cloudflare/Workers/HttpServer.ts
@@ -1,8 +1,10 @@
 import type * as cf from "@cloudflare/workers-types";
 import * as Deferred from "effect/Deferred";
 import * as Effect from "effect/Effect";
-import * as Layer from "effect/Layer";
 import * as Option from "effect/Option";
+import * as Scope from "effect/Scope";
+import * as Stream from "effect/Stream";
+import * as HttpBody from "effect/unstable/http/HttpBody";
 import * as EffectHttp from "effect/unstable/http/HttpEffect";
 import * as HttpServerRequest from "effect/unstable/http/HttpServerRequest";
 import * as HttpServerResponse from "effect/unstable/http/HttpServerResponse";
@@ -44,10 +46,8 @@ export const makeRequestEffect = <Req = never>(
     });
 
     return yield* toHandledWebResponse(safeHandler).pipe(
-      Effect.provide([
-        Layer.succeed(HttpServerRequest.HttpServerRequest, request),
-        Layer.succeed(Request, webRequest as any),
-      ]),
+      Effect.provideService(HttpServerRequest.HttpServerRequest, request),
+      Effect.provideService(Request, webRequest as any),
     );
   }) as any;
 };
@@ -59,18 +59,29 @@ const toHandledWebResponse = <Req>(
     // `toHandled` exposes the final response through this callback, not its
     // return value. Keep the assignment isolated here so callers get Response.
     const context = yield* Effect.context();
+    const eventScope = yield* Scope.Scope;
     const webResponse = yield* Deferred.make<Response>();
 
+    const toWeb = (request: HttpServerRequest.HttpServerRequest, response: HttpServerResponse.HttpServerResponse) => {
+      let transferred = request.method === "HEAD" ? response : EffectHttp.scopeTransferToStream(response);
+      const streaming = request.method !== "HEAD" && transferred.body instanceof HttpBody.Stream;
+      if (streaming && transferred.body instanceof HttpBody.Stream) {
+        const body = transferred.body;
+        // The HTTP scope (and its root span) closes first. The outer event
+        // exporter closes only after that stream finalizer has completed.
+        transferred = HttpServerResponse.setBodyKeepHeaders(transferred, HttpBody.stream(
+          body.stream.pipe(Stream.onExit((exit) => Scope.close(eventScope, exit))),
+          body.contentType,
+          body.contentLength,
+        ));
+      }
+      const web = HttpServerResponse.toWeb(transferred, { withoutBody: request.method === "HEAD", context });
+      if (streaming) EffectHttp.scopeDisableClose(eventScope);
+      return web;
+    };
     yield* EffectHttp.toHandled(handler, (request, response) =>
-      Deferred.succeed(
-        webResponse,
-        // Conversion to web response with options matches `EffectHttp.toWebHandler`'s callback.
-        HttpServerResponse.toWeb(EffectHttp.scopeTransferToStream(response), {
-          withoutBody: request.method === "HEAD",
-          context,
-        }),
-      ),
-    );
+      Deferred.succeed(webResponse, toWeb(request, response)),
+    ).pipe(Effect.forkScoped);
     return yield* Deferred.await(webResponse);
   });
 
diff --git a/src/Cloudflare/Workers/cloudflare_workers.ts b/src/Cloudflare/Workers/cloudflare_workers.ts
--- a/src/Cloudflare/Workers/cloudflare_workers.ts
+++ b/src/Cloudflare/Workers/cloudflare_workers.ts
@@ -11,8 +11,9 @@
 // while TypeScript infers the catch result.
 const moduleImport: Promise<typeof import("cloudflare:workers")> =
   import("cloudflare:workers");
+let tracing: typeof import("cloudflare:workers").tracing | undefined;
 const modulePromise: Promise<typeof import("cloudflare:workers")> =
-  moduleImport.catch(
+  moduleImport.then((module) => { tracing = module.tracing; return module; }).catch(
     () =>
       ({
         env: {},
@@ -27,3 +28,7 @@
   Effect.promise(() => modulePromise);
 
 export default cloudflare_workers;
+
+/** Native invocation spans also cover work before the Effect tracer exists. */
+export const platformSpan = <A>(name: string, run: () => A): A =>
+  tracing?.enterSpan === undefined ? run() : tracing.enterSpan(name, () => run());
diff --git a/lib/Cloudflare/Workers/cloudflare_workers.js b/lib/Cloudflare/Workers/cloudflare_workers.js
--- a/lib/Cloudflare/Workers/cloudflare_workers.js
+++ b/lib/Cloudflare/Workers/cloudflare_workers.js
@@ -9,7 +9,8 @@
 // Type the import before chaining to avoid expanding the full module namespace
 // while TypeScript infers the catch result.
 const moduleImport = import("cloudflare:workers");
-const modulePromise = moduleImport.catch(() => ({
+let tracing;
+const modulePromise = moduleImport.then((module) => { tracing = module.tracing; return module; }).catch(() => ({
     env: {},
     DurableObject: class {
     },
@@ -19,4 +20,6 @@
 }));
 const cloudflare_workers = Effect.promise(() => modulePromise);
 export default cloudflare_workers;
-//# sourceMappingURL=cloudflare_workers.js.map
\ No newline at end of file
+//# sourceMappingURL=cloudflare_workers.js.map
+export const platformSpan = (name, run) =>
+  tracing?.enterSpan === undefined ? run() : tracing.enterSpan(name, () => run());
diff --git a/lib/Cloudflare/Workers/cloudflare_workers.d.ts b/lib/Cloudflare/Workers/cloudflare_workers.d.ts
--- a/lib/Cloudflare/Workers/cloudflare_workers.d.ts
+++ b/lib/Cloudflare/Workers/cloudflare_workers.d.ts
@@ -1,4 +1,5 @@
 import * as Effect from "effect/Effect";
 declare const cloudflare_workers: Effect.Effect<typeof import("cloudflare:workers")>;
 export default cloudflare_workers;
-//# sourceMappingURL=cloudflare_workers.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=cloudflare_workers.d.ts.map
+export declare const platformSpan: <A>(name: string, run: () => A) => A;
diff --git a/src/Cloudflare/Workers/WorkerBridge.ts b/src/Cloudflare/Workers/WorkerBridge.ts
--- a/src/Cloudflare/Workers/WorkerBridge.ts
+++ b/src/Cloudflare/Workers/WorkerBridge.ts
@@ -21,7 +21,7 @@
 import { StackContext } from "../../StackContext.ts";
 import { buildEventTelemetry } from "../../TelemetryRuntime.ts";
 import { CloudflareEnvironment } from "../CloudflareEnvironmentService.ts";
-import cloudflare_workers from "./cloudflare_workers.ts";
+import cloudflare_workers, { platformSpan } from "./cloudflare_workers.ts";
 import { isScopeEjected } from "./HttpServer.ts";
 import {
   ErrorTag,
@@ -88,11 +88,12 @@
     ) => T | Promise<T>,
   ): Promise<T> => {
     const scope = Scope.makeUnsafe();
-    return build((promise) => ctx.waitUntil(promise as Promise<any>))
+    return platformSpan("alchemy.response", () =>
+      platformSpan("alchemy.runtime.wait", () => build((promise) => ctx.waitUntil(promise as Promise<any>)))
       .then(
         (built) => {
           const [eff, services] = makeEffect(built);
-          return eff.pipe(
+          return platformSpan("alchemy.handler", () => eff.pipe(
             // Per-event services take precedence over the captured services
             // and the built isolate context: the isolate context carries the
             // *deferred* WorkerExecutionContext (yieldable in the top-level
@@ -123,13 +124,13 @@
               ),
             ),
             Effect.runPromiseExit,
-          );
+          ));
         },
         // A failed isolate build reaches callers as a defect exit so the RPC
         // path can envelope-encode it like any other handler defect.
         (error) => Exit.die(error),
       )
-      .then((exit) => onExit(exit, scope))
+      .then((exit) => onExit(exit, scope)))
       .finally(() =>
         isScopeEjected(scope)
           ? undefined
@@ -140,7 +141,7 @@
               // reaches the telemetry exporter's buffer before the scope's
               // flush finalizer runs.
               new Promise((resolve) => setTimeout(resolve, 0)).then(() =>
-                Effect.runPromise(Scope.close(scope, Exit.void)),
+                platformSpan("alchemy.cleanup", () => Effect.runPromise(Scope.close(scope, Exit.void))),
               ),
             ),
       );
@@ -356,7 +357,7 @@
    * `Config` read in user init) resets the memo and heals on the next event.
    */
   shared = (pin) => {
-    const promise = (built ??= Effect.runPromise(
+    const promise = (built ??= platformSpan("alchemy.runtime.initialize", () => Effect.runPromise(
       Layer.buildWithMemoMap(globalContext, memoMap, instanceScope).pipe(
         // Strip the build's memo map from the exposed context so a Layer the
         // user `Effect.provide`s *inside a handler* builds per event instead
@@ -364,7 +365,7 @@
         // across concurrent events.
         Effect.map(Context.omit(Layer.CurrentMemoMap)),
       ),
-    ).catch((error) => {
+     )).catch((error) => {
       built = undefined;
       throw error;
     }));
diff --git a/lib/Cloudflare/Workers/WorkerBridge.js b/lib/Cloudflare/Workers/WorkerBridge.js
--- a/lib/Cloudflare/Workers/WorkerBridge.js
+++ b/lib/Cloudflare/Workers/WorkerBridge.js
@@ -16,7 +16,7 @@
 import { StackContext } from "../../StackContext.js";
 import { buildEventTelemetry } from "../../TelemetryRuntime.js";
 import { CloudflareEnvironment } from "../CloudflareEnvironmentService.js";
-import cloudflare_workers from "./cloudflare_workers.js";
+import cloudflare_workers, { platformSpan } from "./cloudflare_workers.js";
 import { isScopeEjected } from "./HttpServer.js";
 import { ErrorTag, encodeRpcError, toRpcStream, } from "./Rpc.js";
 import { ExportedHandlerMethods, WorkerEnvironment, WorkerExecutionContext, deferredExecutionContext, fromExecutionContext, } from "./WorkerRuntime.js";
@@ -31,10 +31,10 @@
     });
     const processEvent = (makeEffect, ctx, env, onExit) => {
         const scope = Scope.makeUnsafe();
-        return build((promise) => ctx.waitUntil(promise))
+        return platformSpan("alchemy.response", () => platformSpan("alchemy.runtime.wait", () => build((promise) => ctx.waitUntil(promise)))
             .then((built) => {
             const [eff, services] = makeEffect(built);
-            return eff.pipe(
+            return platformSpan("alchemy.handler", () => eff.pipe(
             // Per-event services take precedence over the captured services
             // and the built isolate context: the isolate context carries the
             // *deferred* WorkerExecutionContext (yieldable in the top-level
@@ -50,12 +50,12 @@
             // so buffered telemetry flushes when the scope closes into
             // `ctx.waitUntil` below — never on workerd's ephemeral
             // isolate scope.
-            Layer.effectContext(buildEventTelemetry(built.context, scope, built.telemetry()))).pipe(Layer.provideMerge(Layer.succeedContext(services)), Layer.provideMerge(Layer.succeedContext(built.context)))), Effect.runPromiseExit);
+            Layer.effectContext(buildEventTelemetry(built.context, scope, built.telemetry()))).pipe(Layer.provideMerge(Layer.succeedContext(services)), Layer.provideMerge(Layer.succeedContext(built.context)))), Effect.runPromiseExit));
         }, 
         // A failed isolate build reaches callers as a defect exit so the RPC
         // path can envelope-encode it like any other handler defect.
         (error) => Exit.die(error))
-            .then((exit) => onExit(exit, scope))
+            .then((exit) => onExit(exit, scope)))
             .finally(() => isScopeEjected(scope)
             ? undefined
             : ctx.waitUntil(
@@ -64,7 +64,7 @@
             // Yield one macrotask before closing the scope so that span
             // reaches the telemetry exporter's buffer before the scope's
             // flush finalizer runs.
-            new Promise((resolve) => setTimeout(resolve, 0)).then(() => Effect.runPromise(Scope.close(scope, Exit.void)))));
+            new Promise((resolve) => setTimeout(resolve, 0)).then(() => platformSpan("alchemy.cleanup", () => Effect.runPromise(Scope.close(scope, Exit.void))))));
     };
     class WorkerBridge extends Base {
         ctx;
@@ -194,12 +194,12 @@
      * `Config` read in user init) resets the memo and heals on the next event.
      */
     shared = (pin) => {
-        const promise = (built ??= Effect.runPromise(Layer.buildWithMemoMap(globalContext, memoMap, instanceScope).pipe(
+        const promise = (built ??= platformSpan("alchemy.runtime.initialize", () => Effect.runPromise(Layer.buildWithMemoMap(globalContext, memoMap, instanceScope).pipe(
         // Strip the build's memo map from the exposed context so a Layer the
         // user `Effect.provide`s *inside a handler* builds per event instead
         // of sharing one instance (pinned to the first request's IoContext)
         // across concurrent events.
-        Effect.map(Context.omit(Layer.CurrentMemoMap)))).catch((error) => {
+        Effect.map(Context.omit(Layer.CurrentMemoMap))))).catch((error) => {
             built = undefined;
             throw error;
         }));
diff --git a/src/Cloudflare/Workers/DurableObjectBridge.ts b/src/Cloudflare/Workers/DurableObjectBridge.ts
--- a/src/Cloudflare/Workers/DurableObjectBridge.ts
+++ b/src/Cloudflare/Workers/DurableObjectBridge.ts
@@ -20,6 +20,7 @@
 } from "./DurableObjectState.ts";
 import { isScopeEjected, makeRequestEffect } from "./HttpServer.ts";
 import { fromWebSocket } from "./WebSocket.ts";
+import { platformSpan } from "./cloudflare_workers.ts";
 import { getWorkerExport, handleRpcExit } from "./WorkerBridge.ts";
 
 /**
@@ -65,7 +66,7 @@
         const instanceScope = Scope.makeUnsafe();
 
         this.#instance = state.blockConcurrencyWhile(() =>
-          build((promise) => void (state as any).waitUntil?.(promise)).then(
+          platformSpan("alchemy.do.initialize", () => build((promise) => void (state as any).waitUntil?.(promise)).then(
             ({ context, export: exported, telemetry }) => {
               const { constructor, services } = exported;
               const doContext = Layer.mergeAll(
@@ -92,7 +93,7 @@
                 Effect.runPromise,
               );
             },
-          ),
+          )),
         );
 
         return new Proxy(this, {
@@ -136,9 +137,9 @@
       ) {
         const scope = Scope.makeUnsafe();
 
-        const { instance, services, context, telemetry } = await this.#instance;
-
-        return fn(instance)
+        const { instance, services, context, telemetry } = await platformSpan("alchemy.do.wait", () => this.#instance);
+
+        return platformSpan("alchemy.do.response", () => fn(instance)
           .pipe(
             Effect.provide(
               Layer.mergeAll(
@@ -167,7 +168,7 @@
               : exit._tag === "Success"
                 ? Promise.resolve(exit.value)
                 : Promise.reject(Cause.squash(exit.cause)),
-          )
+          ))
           .finally(() =>
             isScopeEjected(scope)
               ? undefined
@@ -176,7 +177,7 @@
                   // HttpMiddleware tracer's late span-end reaches the
                   // telemetry exporter before the scope's flush finalizer.
                   new Promise((resolve) => setTimeout(resolve, 0)).then(() =>
-                    Effect.runPromise(Scope.close(scope, Exit.void)),
+                    platformSpan("alchemy.cleanup", () => Effect.runPromise(Scope.close(scope, Exit.void))),
                   ),
                 ),
           );
diff --git a/lib/Cloudflare/Workers/DurableObjectBridge.js b/lib/Cloudflare/Workers/DurableObjectBridge.js
--- a/lib/Cloudflare/Workers/DurableObjectBridge.js
+++ b/lib/Cloudflare/Workers/DurableObjectBridge.js
@@ -9,6 +9,7 @@
 import { DurableObjectState, fromDurableObjectState, } from "./DurableObjectState.js";
 import { isScopeEjected, makeRequestEffect } from "./HttpServer.js";
 import { fromWebSocket } from "./WebSocket.js";
+import { platformSpan } from "./cloudflare_workers.js";
 import { getWorkerExport, handleRpcExit } from "./WorkerBridge.js";
 /**
  * Create a DurableObjectBridge class that proxies RPC method calls through
@@ -34,7 +35,7 @@
             super(state, env);
             this.#state = state;
             const instanceScope = Scope.makeUnsafe();
-            this.#instance = state.blockConcurrencyWhile(() => build((promise) => void state.waitUntil?.(promise)).then(({ context, export: exported, telemetry }) => {
+            this.#instance = state.blockConcurrencyWhile(() => platformSpan("alchemy.do.initialize", () => build((promise) => void state.waitUntil?.(promise)).then(({ context, export: exported, telemetry }) => {
                 const { constructor, services } = exported;
                 const doContext = Layer.mergeAll(Layer.succeed(DurableObjectState, fromDurableObjectState(this.#state)), Layer.succeed(Scope.Scope, instanceScope)).pipe(Layer.provideMerge(Layer.succeedContext(services)), Layer.provideMerge(Layer.succeedContext(context)));
                 return constructor.pipe(Effect.provide(doContext), Effect.flatMap((instance) => instance.pipe(Effect.provide(doContext))), Effect.onExit((exit) => Exit.isFailure(exit) ? Scope.close(instanceScope, exit) : Effect.void), Effect.map((instance) => ({
@@ -43,7 +44,7 @@
                     context,
                     telemetry,
                 })), Effect.runPromise);
-            }));
+            })));
             return new Proxy(this, {
                 get: (target, prop) => {
                     const bind = (f) => typeof f === "function" ? f.bind(target) : f;
@@ -79,8 +80,8 @@
         }
         async #execute(fn, onExit) {
             const scope = Scope.makeUnsafe();
-            const { instance, services, context, telemetry } = await this.#instance;
-            return fn(instance)
+            const { instance, services, context, telemetry } = await platformSpan("alchemy.do.wait", () => this.#instance);
+            return platformSpan("alchemy.do.response", () => fn(instance)
                 .pipe(Effect.provide(Layer.mergeAll(Layer.succeed(DurableObjectState, fromDurableObjectState(this.#state)), Layer.succeed(Scope.Scope, scope), 
             // The configured telemetry exporters, attached to the *call*
             // scope by `buildEventTelemetry` so buffered telemetry
@@ -91,14 +92,14 @@
                 ? onExit(exit, scope)
                 : exit._tag === "Success"
                     ? Promise.resolve(exit.value)
-                    : Promise.reject(Cause.squash(exit.cause)))
+                    : Promise.reject(Cause.squash(exit.cause))))
                 .finally(() => isScopeEjected(scope)
                 ? undefined
                 : this.ctx.waitUntil(
                 // Match WorkerBridge: yield one macrotask so the
                 // HttpMiddleware tracer's late span-end reaches the
                 // telemetry exporter before the scope's flush finalizer.
-                new Promise((resolve) => setTimeout(resolve, 0)).then(() => Effect.runPromise(Scope.close(scope, Exit.void)))));
+                new Promise((resolve) => setTimeout(resolve, 0)).then(() => platformSpan("alchemy.cleanup", () => Effect.runPromise(Scope.close(scope, Exit.void))))));
         }
         async fetch(request) {
             return this.#execute((instance) => instance.fetch
diff --git a/src/Cloudflare/Workers/cloudflare_workers.ts b/src/Cloudflare/Workers/cloudflare_workers.ts
--- a/src/Cloudflare/Workers/cloudflare_workers.ts
+++ b/src/Cloudflare/Workers/cloudflare_workers.ts
@@ -30,5 +30,18 @@
 export default cloudflare_workers;
 
 /** Native invocation spans also cover work before the Effect tracer exists. */
-export const platformSpan = <A>(name: string, run: () => A): A =>
-  tracing?.enterSpan === undefined ? run() : tracing.enterSpan(name, () => run());
+export const platformSpan = <A>(name: string, run: () => Promise<A>): Promise<A> => {
+  // Tail delivery gives each invocation a timing record even when native traces are sampled.
+  // Cloudflare clocks advance on I/O: CPU cost remains the platform's separate cpuTime field.
+  const measured = () => {
+    const started = performance.now();
+    return Promise.resolve().then(run).finally(() => {
+      if (name !== "alchemy.handler") console.log({
+        type: "alchemy.phase",
+        name,
+        durationMs: Math.max(0, performance.now() - started),
+      });
+    });
+  };
+  return tracing?.enterSpan === undefined ? measured() : tracing.enterSpan(name, measured);
+};
diff --git a/lib/Cloudflare/Workers/cloudflare_workers.js b/lib/Cloudflare/Workers/cloudflare_workers.js
--- a/lib/Cloudflare/Workers/cloudflare_workers.js
+++ b/lib/Cloudflare/Workers/cloudflare_workers.js
@@ -21,5 +21,18 @@
 const cloudflare_workers = Effect.promise(() => modulePromise);
 export default cloudflare_workers;
 //# sourceMappingURL=cloudflare_workers.js.map
-export const platformSpan = (name, run) =>
-  tracing?.enterSpan === undefined ? run() : tracing.enterSpan(name, () => run());
+export const platformSpan = (name, run) => {
+  // Tail delivery gives each invocation a timing record even when native traces are sampled.
+  // Cloudflare clocks advance on I/O: CPU cost remains the platform's separate cpuTime field.
+  const measured = () => {
+    const started = performance.now();
+    return Promise.resolve().then(run).finally(() => {
+      if (name !== "alchemy.handler") console.log({
+        type: "alchemy.phase",
+        name,
+        durationMs: Math.max(0, performance.now() - started),
+      });
+    });
+  };
+  return tracing?.enterSpan === undefined ? measured() : tracing.enterSpan(name, measured);
+};
diff --git a/lib/Cloudflare/Workers/cloudflare_workers.d.ts b/lib/Cloudflare/Workers/cloudflare_workers.d.ts
--- a/lib/Cloudflare/Workers/cloudflare_workers.d.ts
+++ b/lib/Cloudflare/Workers/cloudflare_workers.d.ts
@@ -2,4 +2,4 @@
 declare const cloudflare_workers: Effect.Effect<typeof import("cloudflare:workers")>;
 export default cloudflare_workers;
 //# sourceMappingURL=cloudflare_workers.d.ts.map
-export declare const platformSpan: <A>(name: string, run: () => A) => A;
+export declare const platformSpan: <A>(name: string, run: () => Promise<A>) => Promise<A>;
diff --git a/src/Cloudflare/Workers/LocalWorkerProvider.ts b/src/Cloudflare/Workers/LocalWorkerProvider.ts
index 02757fb2cfd6cb7eb0c7af3ae4bc0ae2699e0b43..24e4d8049366a0725dd6e4b06c8cc08fbd265734 100644
--- a/src/Cloudflare/Workers/LocalWorkerProvider.ts
+++ b/src/Cloudflare/Workers/LocalWorkerProvider.ts
@@ -583,7 +583,7 @@ export const LocalWorkerProvider = () =>
         config: WorkerConfig,
         selfUrl: string | undefined,
       ) {
-        const { accountId } = yield* cloudflareEnv;
+        const { accountId } = yield* (Object.values(config.devRemote).some(Boolean) ? cloudflareEnv : Effect.succeed({ accountId: "local" }));
         return yield* materializeRuntimeBindings(
           {
             ...config,
@@ -1250,7 +1250,7 @@ export const LocalWorkerProvider = () =>
               // The dev server and its workerd run in a child process rooted
               // at the app.
               const root = path.resolve(rootDir ?? process.cwd());
-              const { accountId } = yield* cloudflareEnv;
+              const { accountId } = yield* (Object.values(worker.devRemote ?? {}).some(Boolean) ? cloudflareEnv : Effect.succeed({ accountId: "local" }));
               // Queue-consumer wiring can change while the child is starting
               // (a sibling `Consumer` reconcile), before the restart hook
               // below exists to pick it up. We hold the serve lock, so a
@@ -1457,7 +1457,10 @@ export const LocalWorkerProvider = () =>
           const durableObjectNamespaces: Record<string, string> = {};
           for (const { data } of bindings) {
             for (const binding of data?.bindings ?? []) {
-              if (binding.type === "durable_object_namespace") {
+              // Foreign bindings may contain unresolved script-name Outputs during precreate.
+              // Their namespace belongs to the host Worker and must not be declared here.
+              if (binding.type === "durable_object_namespace" &&
+                  (!binding.scriptName || binding.scriptName === name)) {
                 durableObjectNamespaces[binding.className] =
                   binding.namespaceId ??
                   encodeURIComponent(
@@ -1466,7 +1469,7 @@ export const LocalWorkerProvider = () =>
               }
             }
           }
-          const { accountId } = yield* cloudflareEnv;
+          const { accountId } = yield* (bindings.some(({ data }) => Object.values(data?.devRemote ?? {}).some(Boolean)) ? cloudflareEnv : Effect.succeed({ accountId: "local" }));
           const urls =
             news.dev?.mode === "external"
               ? // news.dev.url may be an unresolved output; avoid trying to resolve it here.
@@ -1498,7 +1501,7 @@ export const LocalWorkerProvider = () =>
         }),
 
         start: Effect.fn(function* ({ fqn, config, invalidate }) {
-          const { accountId } = yield* cloudflareEnv;
+          const { accountId } = yield* (Object.values(config.devRemote).some(Boolean) ? cloudflareEnv : Effect.succeed({ accountId: "local" }));
 
           // `dev: { mode: "external" }` opts out of running a local Worker
           // entirely — typically because an external dev process
diff --git a/src/Cloudflare/Workflows/Workflow.ts b/src/Cloudflare/Workflows/Workflow.ts
--- a/src/Cloudflare/Workflows/Workflow.ts
+++ b/src/Cloudflare/Workflows/Workflow.ts
@@ -1121,3 +1121,3 @@
     diff: Effect.fn(function* ({ news, output }) {
-      const { accountId } = yield* yield* CloudflareEnvironment;
+      const accountId = output?.accountId ?? "local";
       if (!output?.workflowId) return { action: "update" } as const;
@@ -1135,3 +1135,3 @@
     reconcile: Effect.fn(function* ({ news, output }) {
-      const { accountId } = yield* yield* CloudflareEnvironment;
+      const accountId = output?.accountId ?? "local";
       return {
diff --git a/lib/Cloudflare/Workflows/Workflow.js b/lib/Cloudflare/Workflows/Workflow.js
--- a/lib/Cloudflare/Workflows/Workflow.js
+++ b/lib/Cloudflare/Workflows/Workflow.js
@@ -595,3 +595,3 @@
     diff: Effect.fn(function* ({ news, output }) {
-        const { accountId } = yield* yield* CloudflareEnvironment;
+        const accountId = output?.accountId ?? "local";
         if (!output?.workflowId)
@@ -611,3 +611,3 @@
     reconcile: Effect.fn(function* ({ news, output }) {
-        const { accountId } = yield* yield* CloudflareEnvironment;
+        const accountId = output?.accountId ?? "local";
         return {
