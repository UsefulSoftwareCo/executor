diff --git a/install.js b/install.js
index 285a641d10535c5c17467c088f50a87ad9159124..af2503b69188c55227dbfbf1f69fadc50196d196 100644
--- a/install.js
+++ b/install.js
@@ -262,7 +262,9 @@ function spawn(cmd, args, options = {}) {
 __name(spawn, "spawn");
 
 // src/platform.ts
-var os2 = process.platform, arch = os2 === "darwin" && process.arch === "x64" && isRosetta2() ? "arm64" : process.arch, abi = os2 === "android" ? "android" : os2 === "linux" && isLinuxMusl() ? "musl" : void 0, platforms = [
+// Keep the selected execution architecture when building Intel artifacts under Rosetta.
+// The dependency resolver installed optional binaries for process.arch, not the host CPU.
+var os2 = process.platform, arch = process.arch, abi = os2 === "android" ? "android" : os2 === "linux" && isLinuxMusl() ? "musl" : void 0, platforms = [
   {
     os: "darwin",
     arch: "arm64",
