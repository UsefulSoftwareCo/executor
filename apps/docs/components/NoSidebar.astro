---
// Blume's `layout.Sidebar` slot, rendering nothing. The documentation tree is
// in the marketing rail (see DocsRail.astro), so Blume's own navigation column
// would be a second copy of it. styles.css takes the empty column out of the
// grid.
---
