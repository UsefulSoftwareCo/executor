import { HostedNavigation } from "@executor-js/hosted-web/navigation";

/** SelfHost navigation; add this host's links alongside the common pages. */
export function Navigation() {
  return <HostedNavigation />;
}
