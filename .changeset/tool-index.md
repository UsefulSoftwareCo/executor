---
"apps": patch
"@executor-js/sdk": patch
---

List an app's tools without their schemas and describe one tool on demand.
`tools.index` returns names and descriptions, `tools.get` returns one tool's
schemas, and MCP and GraphQL catalogs store summaries beside their full pages.
