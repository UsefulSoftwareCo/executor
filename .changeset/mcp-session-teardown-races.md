---
"@executor-js/cloudflare": patch
---

Answer owner checks with the reconnect verdict while a session Durable Object is mid-destroy, and initialize a fresh session instance through PartyServer's gate when an RPC restores its runtime, so the next request does not rebuild it.
